def countotalaverage(values):
    Count = len(values)
    Total = sum(values)
    average = sum(values) / len(values)
    return (Count, Total, average)

count = 0
sum = 0
input = input('Number')
for value in input:
    count = 
    sum = sum + float(value)